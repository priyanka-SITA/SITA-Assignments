//JavaScript Program to Check if An Array Contains a Specified Value

var arr = ["ant", "pot", "van", "cat"];
var arr2 = arr.includes("pat");
if (arr2) {
  console.log("Array contains a value");
} else {
  console.log("Array does not contain value");
}
